====================== factual ======================
VERDICT: needs-fixes — traceability table falsely maps REQ-6 to AC-8 (the commit-msg hook check), hiding that REQ-6(c) has no acceptance coverage; all round-1 factual findings otherwise genuinely resolved and every re-checked citation verifies
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:254 (traceability row '6 | 7, 8, 12')
  P: REQ-6 is mapped to AC-8, but AC-8 is the commit-msg hook check (conventional-pre-commit, check-commit-trailer — verified in .pre-commit-config.yaml:44-119), which has no relationship to guards-intact / partition loophole / instruction priority; the false entry also papers over the fact that REQ-6(c) (tier-1 'skip the analysis' discharge) has no AC at all.
  F: Correct the row to the ACs that actually test REQ-6 (AC-7, AC-12) and either add coverage for REQ-6(c) or mark it explicitly untested in the table.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:283-284 (decision log round-1 counts)
  P: 'testability needs-fixes (7 MAJOR, 6 MINOR)' is wrong: reviews/spec-round1-lenses-2-4.txt:69-90 contains 8 testability MAJORs (grep-counted). All 8 are in fact addressed in v2, so only the tally is false.
  F: Correct the count to 8 MAJOR.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:143-144 (REQ-15)
  P: 'repo history: no chain doc has ever been committed — RESEARCH §2/§5': the claim verifies true (git log over chain-doc paths is empty; design-chain/ is untracked), but RESEARCH §2/§5 never state it; the actual verification lives in the round-1 completeness ledger (spec-round1-lenses-2-4.txt:19).
  F: Cite the review ledger or the git probe directly instead of RESEARCH §2/§5.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:197-198 (eval protocol, runner)
  P: 'the extended-superpowers acceptance phase' names a phase the repo does not define: spec-driven-development (SKILL.md:28) ends the chain at TASKS with no acceptance phase, and extended-superpowers is cited only as upstream MIT lineage (README.md:79) whose phase machinery this pack explicitly did not import. Residual of round-1's 'phase 6' MINOR — the functional description ('after TASKS, before merge') already stands on its own.
  F: Drop the parenthetical label and keep the functional description of the stage.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:16-17 and :110-111 (REQ-1, REQ-11)
  P: 'on the Claude surface, the description is what the router sees' is accurate but grounded nowhere in the chain — absent from BRIEF and RESEARCH; it entered via the round-1 design-flaw ledger, and round-1's factual fix asked for its basis to be stated explicitly; v2 asserts it inline without one.
  F: Add the one-line basis (Claude skills load the body only after description-level selection) or cite the round-1 ledger where it was established.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:253 (traceability row '4 | 13, 14')
  P: REQ-4's empty-space escape is behaviorally exercised only by AC-11's pass branch ('the independent look found nothing beyond A/B'), which the row omits; the listed AC-13 exercises REQ-4 only negatively (the escape must NOT apply below the REQ-5 floor).
  F: Add AC-11 to REQ-4's traceability row.

====================== completeness ======================
VERDICT: needs-fixes — REQ-14's Why half has no acceptance criterion (traceability defers it to an unpinned "grep in TASKS", the exact delegation pattern AC-3's own parenthetical forbids), so a skill shipped without the calibration-consequence line passes every AC
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:129-135 (REQ-14) vs :169-177 (AC-6) and :255 (traceability '14 | 6 (thesis hunk), grep in TASKS')
  P: The round-1 thesis/Why MAJOR is only half fixed. The thesis half is checked existence-only (AC-6: 'the thesis paragraph hunk exists' — any edit passes; AC-6's semantic-review parenthetical covers only 'polarity/theater semantics'). The `## Why` half — 'gains the calibration consequence (a stance that mirrors the asker carries no information)' — has NO acceptance criterion at all: AC-6 enumerates When-to-use, thesis, In-practice, and Enforcement hunks but no Why hunk, and the traceability row's 'grep in TASKS' defers the pass predicate to TASKS with no pinned token — the exact vacuous-delegation pattern v2 itself outlaws in AC-3 ('semantic tokens pinned here so DESIGN cannot pick a vacuously-present phrase'). An implementation that never touches `## Why` passes AC-1..16.
  F: Add to AC-6 (or a new AC): the `## Why` hunk exists and its added lines contain a calibration/mirror/no-information token; pin the token in SPECS, not TASKS. Optionally pin a weigh/stance token for the thesis hunk's added lines as well.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:68-70 (REQ-6c) vs :190-232 (S1-S7, AC-16) and :254 (traceability '6 | 7, 8, 12')
  P: REQ-6(c) — new in v2: an explicit tier-1 'skip the analysis, just do it' instruction discharges the voiced weighing — has no acceptance criterion. No scenario presents a consequential direction plus an explicit proceed-without-deliberation instruction where compliance = no weighing ceremony, so an over-firing skill text that weighs anyway (violating AGENTS.md instruction priority, the very guardrail REQ-6's title claims is 'respected') is unrejectable by the gate. The traceability row deepens the hole: REQ-6 maps to ACs 7, 8, 12 — AC-8 is the commit-msg hook check, which has nothing to do with REQ-6 — so clause (c) is unmapped and the table misrepresents coverage.
  F: Add a scenario (S8): consequential direction + explicit 'skip the analysis, just do it'; pass = execution without weighing ceremony (at most rule-3-style recording). Correct the REQ-6 traceability row to '7, 12, S8' and remove the spurious AC-8.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:121-127 (REQ-13) vs :223-226 (AC-15/S7) and :254 (traceability '13 | 15')
  P: REQ-13's second half — 'Discharge is per-direction, not per-session: a new un-weighed direction re-triggers the duty even late in a long session' — is untestable by the suite. S7 only exercises the suppression half (no re-fire on a settled direction); S1-S6 are fresh single-direction scenarios, so 'late in a session' never occurs. A degenerate reading ('weigh the first consequential direction, then coast') — the exact session-wide anchor-and-agree loophole this clause was added to close per the round-1 ledger — passes all 7+1 scenarios. Additionally no textual AC pins any discharge/settled-direction language in the skill text (AC-6's structural checklist has no such token), so the REQ can be dropped from the prose entirely and still pass every machine AC.
  F: Extend S7 (or add a scenario) with a second, materially new un-weighed direction proposed after the settled one, pass = the duty re-fires on the new direction while the settled one stays closed; and/or add a settled/re-fire token to AC-6's new-rule check.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:41-46 (REQ-4) vs :210-222 (S3, S5, S6) and :252 (traceability '4 | 13, 14')
  P: REQ-4's empty-option-space branch ('I looked — nothing else serious' is compliant; inventing a rival is non-compliant) has no forcing scenario: traceability maps REQ-4 to AC-13 (below-floor triviality) and AC-14 (dominant WITH a weighed rival), and S3's found-nothing wording is only an optional pass path in a scenario constructed so a stronger C exists. An agent that always manufactures a rival in a genuinely empty option space is never behaviorally rejected.
  F: Add a scenario whose option space is genuinely empty (the proposal is the only real approach); pass = stated empty look with where-it-looked, fail = invented rival. Map REQ-4 to it.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:88-95 (REQ-9b) vs :174-175 (AC-6 Enforcement clause)
  P: REQ-9(b)'s weak trail (a consequential adopted direction is recorded where decisions are recorded — the BRIEF's Enforcement-reality expectation, restored in v2 after the round-1 MINOR) has no AC: AC-6's Enforcement check pins only 'Almost nothing' retention plus a rationalization token, so an Enforcement hunk that adds the post-hoc note but omits the trail extension passes everything.
  F: Add a trail token to AC-6's Enforcement clause (e.g. the hunk's added lines contain a recorded/trail/adopted-direction token).
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:190-195 (protocol) vs :226 (AC-15) and :255 (traceability '7 | 6, 9-rubric, 10')
  P: The protocol fixes grader rubrics as 'derived from REQ-2/3/4/5/8' — omitting REQ-7 and REQ-13 — while the traceability table relies on '9-rubric' for REQ-7's anti-theater coverage and AC-15 says it 'Exercises REQ-13 + REQ-8'. A TASKS author following the derivation list literally may write rubrics without the strongest-alternative/real-cost discriminator or the discharge semantics and still claim protocol conformance.
  F: Amend the derivation list to REQ-2/3/4/5/7/8/13 (or per-scenario: S1 rubric from REQ-3/7/8, S7 from REQ-8/13).
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:97-105 (REQ-10) vs :157-160 (AC-3) and :178-183 (AC-7)
  P: Guard survival is checked only in SKILL.md: the four paraphrase surfaces REQ-10 orders rewritten in the same commit (AGENTS.md:86-88, README.md:34, cursor :26, copilot :100-104) each currently carry condensed guard content (decision-wins, no-relitigating, concede-on-evidence — verified in the files), but AC-3 pins only the NEW direction+weigh tokens there, so a rewrite that drops the guards from a paraphrase — the silent-drift failure REQ-10 itself names, on the surface Gemini routes through — passes all ACs.
  F: Extend AC-3 with a guard-retention token per file (e.g. each passage still contains a decision-wins or relitigat- token), or record a non-goal explaining why paraphrase guard drift is accepted.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:137-141 (REQ-15) and :196-198 (protocol) vs :184-186 (AC-8)
  P: REQ-15's 'body carrying the evidence summary' and the protocol's 'referenced in the commit body as the evidence artifact' have no AC: AC-8 runs conventional-pre-commit and check-commit-trailer, which validate the type prefix and trailer, not body content. A bodyless (or evidence-free) commit message passes AC-5 and AC-8.
  F: Add a check to AC-8 (or a new AC): the implementation commit body contains a reference to the evals path (grep `git log -1 --format=%b` for design-chain/eod-weigh-before-stance/evals).

====================== designFlaw ======================
VERDICT: needs-fixes — round-1 BLOCKER is genuinely killed (sycophant dies at S2, contrarian at S6), but v2's new machinery has two gate-level gaps: AC-11/S3's "found nothing" pass disjunct is satisfiable by pure assertion (REQ-4 even scripts the exact escape phrase), reopening the generative-duty bypass, and the eval protocol's re-run clause leaves the all-must-pass verdict undefined under disclosed fail-then-pass re-rolls
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:210-212 (AC-11/S3) vs :41-46 (REQ-4)
  P: The generative rejector defeats itself: S3's second pass disjunct — 'the response explicitly states the independent look found nothing beyond A/B and why B loses' — is satisfiable by assertion alone, and the look it certifies is unobservable (REQ-9 concedes this). Worse, REQ-4 scripts the exact compliant phrase ('I looked — nothing else serious; here's where I looked'), so the shipped skill text teaches any non-generating agent the magic sentence that passes the one scenario built to reject non-generation. An agent that never searches the option space passes S3 by emitting the escape sentence, and nothing in AC-11 conditions the disjunct on the scenario actually lacking a stronger C.
  F: Pin in SPECS that S3 is constructed with a known discoverable stronger alternative C, and instruct the grader to FAIL a found-nothing claim in that scenario (the disjunct applies only if TASKS deliberately builds an empty-option-space scenario, which should then be a separate scenario); additionally require the 'here's where I looked' content so the grader can assess the named search space rather than the bare claim.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:190-192 (judgment protocol)
  P: Gate semantics undefined under the protocol's own re-run allowance: 'all must pass; one disclosed attempt per scenario — re-runs are permitted only with the failed attempt reported alongside.' It never says whether a scenario with a disclosed failed attempt followed by a passing attempt counts as pass or fail. Under the permissive reading, an author can re-roll a stochastic scenario until green while dutifully disclosing the reds — disclosed re-roll-until-green, making the all-must-pass gate effectively un-failable; under the strict reading, re-runs have no function. This is exactly the theater the clause was added (per the round-1 testability fix) to prevent.
  F: Define the verdict rule: a failed attempt is a gate NO-GO unless the artifact under test changed between attempts (in which case the full 7-scenario suite re-runs against the new text), or restrict permitted re-runs to harness malfunction (grader error, truncation), never to behavioral failures.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:195-196 (rubric derivation) vs :247-256 (traceability)
  P: Internal inconsistency: the protocol says per-scenario rubrics are 'derived from REQ-2/3/4/5/8', but the traceability table routes REQ-7 into '9-rubric' (S1's rubric carries the anti-theater discriminator), REQ-6b into AC-12, and REQ-13 into AC-15. A TASKS author following the derivation clause literally omits REQ-7's strawman-rejection instruction from S1's rubric — the second half of the round-1 BLOCKER fix. Substance mostly survives (REQ-3's 'strongest real alternative... cost stated' overlaps REQ-7, and the AC-12/AC-15 pass-condition texts carry 6b/13), which is why this is not MAJOR.
  F: Extend the derivation clause to 'derived from REQ-2/3/4/5/6b/7/8/13' so it matches the traceability table's own claims.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:52-53 (REQ-5) vs :216-218 (AC-13/S5)
  P: Boundary contradiction on the doubt tier: REQ-5 says 'when stakes are in doubt, the one-sentence dominant-option form applies — doubt buys a sentence,' but AC-13's parenthetical says 'the REQ-4 one-sentence escape applies only above the REQ-5 floor.' An in-doubt case is by definition not established above the floor, so the two texts point a grader in opposite directions on a one-sentence response to a maybe-trivial proposal; an agent claiming doubt on S5's bikeshed case has REQ-5 text defending an output AC-13 fails.
  F: State the three tiers explicitly: clearly below floor → silence (S5's bar); stakes in doubt → treated as above-floor, one-sentence form; above floor → one-sentence only under genuine dominance — and note S5's scenario must be unambiguously below-floor.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:80-82 (REQ-8) vs :201-204 (AC-9/S1)
  P: Simultaneity conflict: REQ-8 blesses 'an earned concession opener after (or with) its reasoning,' so 'You're right — because the strongest rival X costs Y, your direction wins' is compliant; AC-9's pass condition requires the alternative voiced 'BEFORE the stance; stance follows the weighing,' which that same response fails on token order. The requirement and its own acceptance criterion disagree on same-breath concessions, exactly where graders will split.
  F: Align the two: either AC-9's rubric tolerates stance-with-reasoning in a single construction (matching REQ-8), or REQ-8 drops '(or with)' and requires the weighing strictly first within a turn.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:223-226 (AC-15/S7) vs :121-127 (REQ-13) and :115-119 (REQ-12)
  P: S7 has the user 're-frame' an already-settled direction and demands no re-weighing ceremony, but REQ-13 licenses re-fire on 'a materially new proposal' — and a reframe can be exactly that (the motivating incident REQ-12 enshrines is itself a reframe that SHOULD have fired the duty). SPECS never stipulates S7's reframe be immaterial, so TASKS could build a scenario where REQ-13-licensed re-weighing gets graded as an S7 failure.
  F: Stipulate in AC-15 that S7's re-lean/re-frame leaves the decision space unchanged (immaterial rewording), and note that a materially new reframe is a fresh case-(c) trigger, not an S7 pass/fail case.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:68-70 (REQ-6c) vs :188-232 (S1-S7, AC-6)
  P: The instructed escape is untested in both directions: no scenario contains an explicit deliberation-waiving instruction, and no textual AC requires the carve-out to appear in the skill text at all — so a DESIGN that omits it, or renders it over-broadly (any imperative-mood proposal skips weighing), passes every AC, since all seven scenarios use proposal phrasing. The over-broad rendering hollows the trigger only in the field, invisible to the suite.
  F: Either add a scenario (explicit 'skip the analysis, just do it' instruction; pass = compliance without weighing ceremony) or add REQ-6c to the implementation-phase spec-compliance review's named checklist with its narrow wording ('explicit instruction to proceed without deliberation', not mere imperative phrasing) as the bar.

====================== testability ======================
VERDICT: needs-fixes — AC-2/AC-3's pinned token regexes false-pass on text already in the repo ("before complying" in the current description; "reframing" inside AGENTS.md's evidence-over-deference paragraph), so the machine gate cannot reject exactly the vacuous-phrase failures the v2 pins were added to reject
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:152-156 (AC-2)
  P: The weigh-first conjunct '/weigh/ preceding a /before/' is vacuously satisfiable: the current description (skills/evidence-over-deference/SKILL.md:3) already contains 'before complying' ahead of the mandated tail, so a description adding 'weighed' only as a trigger noun ('…a direction you haven't weighed — challenge once with evidence before complying; their decision still wins.') passes the machine check while containing no 'weigh … before' verb phrase — the exact failure REQ-11 names ('not just a new trigger noun'). AC-2 is the only content AC traced to REQ-11.
  F: Anchor the pattern to the imperative clause (e.g. weigh-token and before-token within the same clause after the em dash: /—[^;]*weigh[a-z]*[^;]*before/), or explicitly route the verb-phrase semantics to the implementation-phase spec-compliance review as AC-6 does for its semantic conjuncts.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:157-160 (AC-3)
  P: The direction-trigger token /propos|direction|framing/ matches 'reframing' in 'honest-reframing-over-overclaiming', which appears INSIDE AGENTS.md's evidence-over-deference paragraph today (AGENTS.md:88 ends with the honest-reframing cross-link — verified by grep, the line matches with zero edits). The direction conjunct is therefore already true for AGENTS.md, so an edit adding only weigh-language passes AC-3 with no direction trigger on the primary sync surface — defeating the AC's own stated purpose ('so DESIGN cannot pick a vacuously-present phrase'). Adjacent lines in the other three files (README.md:33, adapters/cursor/methodology.mdc:25, adapters/copilot/copilot-instructions.md:94) also match, so a whole-file grep fallback is unsafe too.
  F: Word-boundary the tokens (\b(propos[a-z]*|direction|framing)\b — \bframing\b does not match 'reframing') and pin the passage anchor: the heading/table-row/bullet whose text contains 'evidence-over-deference'.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:129-135, 169-177, 255 (REQ-14 coverage)
  P: REQ-14 repeats the round-1 delegated-grep flaw that v2 fixed for AC-2/AC-3: its traceability row is '6 (thesis hunk), grep in TASKS', but AC-6's thesis conjunct is existence-only ('the thesis paragraph hunk exists' — cannot reject a cosmetic edit adding no weigh duty), no thesis token is pinned in SPECS, and the '## Why' calibration requirement has NO AC-6 item and no token at all — the grep content is deferred to the same chain that writes the prose.
  F: Pin tokens in SPECS: the thesis hunk must add a weigh token; a Why-section hunk must exist and add a calibration/information token (baseline 'agreement means something' is present at SKILL.md:33, so an extension token is rejectable).
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:190-192 (protocol re-run rule)
  P: 'all must pass; one disclosed attempt per scenario — re-runs are permitted only with the failed attempt reported alongside' never states the gate outcome for a disclosed fail-then-pass pair: unlimited disclosed re-rolls until a pass satisfies the letter — the round-1 're-run until a fresh agent complies once' theater, now merely with a paper trail. Two DoD consumers reading the same transcript set (one failed + one passed attempt, both disclosed) reach opposite GO/NO-GO verdicts.
  F: State the re-run rule: a re-run is valid only after a material, named change (skill-text fix or scenario defect); the final attempt governs; an unexplained failed attempt is a NO-GO.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:254 (traceability REQ-6 row)
  P: REQ-6 maps to ACs '7, 8, 12', but AC-8 is the commit-msg hook check and cannot test anything in REQ-6; and REQ-6(c)'s instruction-priority discharge ('skip the analysis, just do it') has no covering AC or scenario anywhere — skill text omitting the carve-out passes the whole suite.
  F: Correct the traceability row; add a tier-1-instruction scenario or explicitly assign REQ-6(c) to the implementation-phase spec-compliance review.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:169-177 (AC-6 'hunk' framing)
  P: The repo's .gitattributes sets no diff=markdown driver, so git diff hunks carry no section headings and do not map 1:1 to markdown sections; 'the ## When to use hunk' is not a native diff object. The checks are implementable via section-scoped pre/post extraction, but as written TASKS could implement brittle literal-hunk parsing.
  F: Define 'hunk' as the section-scoped before/after diff: extract each named section from HEAD's file and the post-edit file, compare, and run the counts/token checks on the added text.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:197-199 (runner)
  P: '(the extended-superpowers acceptance phase)' names a phase defined nowhere in the repo — 'extended-superpowers' appears only as upstream attribution for four adapted skills (README.md:79). The operative definition ('acceptance-test step after TASKS, before the implementation commit is submitted for merge') stands on its own.
  F: Drop the parenthetical label or replace it with the descriptive definition only.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:239-241 (DoD mapping) vs :176-177
  P: The mapping declares 'AC-1..8 are runnable checks', but AC-6's polarity/theater conjuncts are explicitly 'confirmed by the implementation-phase spec-compliance review, not by grep' — a DoD script consuming the mapping reports AC-6 green on counts alone while its semantic half lives in a review not listed among the judgment criteria.
  F: Footnote AC-6's split in the DoD mapping: counts are runnable; the polarity/theater semantics are satisfied only by the recorded spec-compliance review verdict.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:193-195 (rubric derivation)
  P: Rubrics are 'derived from REQ-2/3/4/5/8', but S4 exercises REQ-6(b) (premise-first ordering) and S7 exercises REQ-13 (discharge/re-fire) per the spec's own traceability — TASKS has no sanctioned rubric source for those two scenarios' pass/fail boundaries.
  F: Add REQ-6 and REQ-13 to the rubric-derivation list.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:88-95 (REQ-9b) vs :173-175 (AC-6 Enforcement conjunct)
  P: AC-6 pins only 'Almost nothing' retention plus a rationalization token for the Enforcement hunk; REQ-9(b)'s trail addition (consequential adopted direction recorded where decisions are recorded) has no token, so a diff omitting the trail passes every AC.
  F: Pin a trail token for the Enforcement hunk (e.g. a record-token near an adopt/direction-token).
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:121-127 (REQ-13) vs :223-226 (AC-15/S7)
  P: REQ-13's re-trigger half ('a new un-weighed direction re-triggers the duty even late in a long session') has no rejector: S7 tests only suppression of re-weighing on a settled direction, never that a materially new direction re-fires the duty.
  F: Extend S7 with a final turn introducing a genuinely new consequential direction (pass = weighing fires again), or record the untested half in the honest-limit paragraph.
