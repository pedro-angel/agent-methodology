====================== completeness ======================
VERDICT: needs-fixes — the AC set cannot reject the spec's own load-bearing requirements: REQ-1's When-to-use body coverage, REQ-3's generative duty, and REQ-6's guard-survival/premise-first ordering all lack acceptance criteria, and the empty-option-space and multi-turn-drift failure modes the BRIEF implies are unspecified.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:11-18 vs :106-108 (AC-6)
  P: REQ-1 mandates the new trigger appear in BOTH the description AND the `## When to use` body 'with a concrete example in the body's existing parenthetical-self-question style', but the AC set never checks the body: AC-2 checks only the frontmatter description, and AC-6's structural checklist (one rule, ≥2 red flags, ≥2 anti-patterns, In-practice addition, Enforcement addition) omits any When-to-use addition. A DESIGN that skips the body example passes every AC while violating REQ-1 — the exact 'routing failure' REQ-1's own rationale warns about. Violates spec-driven-development's bar of acceptance criteria a reviewer can reject.
  F: Add to AC-6 (or a new AC): the `## When to use` paragraph contains the proposes-a-direction trigger with a parenthetical self-question example, greppable phrase set in DESIGN.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:44-49 vs :92-123
  P: REQ-6 ('existing guards survive verbatim in force' — challenge-once, decision-wins, no-relitigating, no-filibuster, rule-1 premise duty; case (a) not displaced) has NO acceptance criterion at all. No machine check that the guard sentences survive (AC-2's `; their decision still wins.` tail covers only the description), and no judgment scenario for the lens-critical mixed case: a proposed direction that ALSO embeds a checkable premise (does the agent check the premise first, per the BRIEF amendment, or jump to weighing?). Governing principle (3) — the skill's own guards must survive — is asserted but unrejectable as written.
  F: Add a machine AC (grep that rules 2-5's guard phrases and rule 1 survive in SKILL.md and the AGENTS.md paragraph) and a judgment AC: a direction embedding a false checkable premise → premise check fires first, then weighing on the residual.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:28-32 vs :113-115 (AC-7)
  P: REQ-3's distinctive content — the alternative must be one the agent GENERATED, 'not merely score the human's option against a runner-up the human happened to mention' — has no acceptance criterion. AC-7 only requires 'weigh an alternative before taking a stance', which a runner-up-scoring agent satisfies. No scenario has the user mention a weak runner-up so the grader can reject an agent that fails the generative duty (the BRIEF amendment's consequence #2).
  F: Add a judgment AC: user proposes direction A and mentions weak runner-up B; compliant behavior generates a stronger alternative C (or explicitly establishes none stronger exists) rather than only scoring A vs B.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:28-36,51-55
  P: No requirement covers the empty-option-space case: when the agent, having genuinely looked, cannot produce ANY real alternative. REQ-3 says the agent 'must produce the strongest real alternative itself'; REQ-4's escape presupposes an alternative was weighed and lost ('naming what was weighed and why it loses'); REQ-7 forbids manufacturing one. In the no-alternative corner the spec forces either a REQ-3 violation or a manufactured strawman (the exact failure the amendment warns the fix could create). The compliant honest output ('I looked for a real alternative and found none — here is where I looked') is unspecified.
  F: Extend REQ-4 (or add a REQ) defining compliant output when no real alternative exists: state that the search happened and came up empty, without inventing a rival; clarify REQ-3's 'duty to look' explicitly licenses this outcome.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:57-62,85-90,110-119 vs BRIEF.md:75-76
  P: Multi-turn drift — the BRIEF's grounding explicitly observes the reflex 'repeated across several turns ("fair," "good pushback") as an opening reflex' — is covered only as narrative (REQ-12's In-practice retelling) and opener-naming (REQ-8). No requirement specifies how the new trigger interacts with rule 5 (no relitigating) across turns: when the human refines or re-proposes an already-weighed/settled direction, does weighing re-fire (risking relitigation) or stay silent (risking the observed per-turn concession drift)? And all judgment ACs (AC-7..9) are single-turn scenario prompts, so the observed failure shape is never exercised.
  F: Add a requirement scoping the trigger per NEW direction (settled directions fall under rule 5; re-fire only on new evidence or a materially new proposal) and at least one multi-turn judgment AC where the user leans repeatedly.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:104-105 (AC-5), :70-76 (REQ-10)
  P: AC-5 ('the diff touches exactly the six files of REQ-10/REQ-11 and nothing else') is unverifiable as specified because the spec is silent on the disposition of the design-chain artifacts themselves. design-chain/ is untracked, not gitignored, and has no landing precedent in repo history (git log over design-chain/ is empty), while the BRIEF requires the change to land 'through its own spec chain' and spec-driven-development rule 4 requires specs to be durable versioned artifacts. If BRIEF/RESEARCH/SPECS/DESIGN/TASKS are committed on the same branch/PR, AC-5 fails or forces an unspecified commit split; if they are not committed, the spec should say where they live.
  F: State the chain artifacts' disposition (committed in a separate docs commit, kept out-of-tree, or gitignored) and scope AC-5 explicitly to the skill-edit commit's diff, not the branch.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:11-18,106-108,127-130 vs skills/evidence-over-deference/SKILL.md:8
  P: The spec never says whether the skill's thesis paragraph (SKILL.md:8) and `## Why` section must widen to cover the weigh-shaped trigger. Both currently frame the skill exclusively around checkable premises ('when a request rests on a premise you can verify, verify it'). REQ-1 mandates description + When-to-use only; AC-6's additive checklist omits the thesis; Non-goals exclude only the H1 rename. A DESIGN that leaves the opening paragraph premise-only satisfies every REQ/AC while shipping a skill whose thesis contradicts its own trigger set — an under-specified WHAT the spec claims as its job (line 6).
  F: Add a requirement (or explicit non-goal with rationale) covering the thesis paragraph and Why section: they must acknowledge the weigh-before-stance case, or DESIGN is told to leave them and why that is coherent.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:78-83 vs :96 (AC-1)
  P: REQ-11's commit-message constraint (`feat(evidence-over-deference): ...` + provenance trailer) has no acceptance criterion: AC-1 (`pre-commit run --all-files`) does not exercise the commit-msg-stage hooks (conventional-pre-commit and check-commit-trailer are `stages: [commit-msg]` in .pre-commit-config.yaml:43-48,115-119), so a wrong commit message passes every listed AC.
  F: Add an AC that validates the commit message (e.g. run the commit-msg hooks against the created commit, or `git log -1` grep in TASKS).
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:64-68 vs BRIEF.md:123-131
  P: The BRIEF's Enforcement-reality expectation of 'at most a weak trail (consequential agreements recorded where decisions are recorded)' is dropped: REQ-9 extends Enforcement only with the post-hoc-rationalization note and neither requires the trail extension (rule 3 currently records only dissent, not consequential adoptions of a proposed direction) nor lists it as a non-goal.
  F: Either extend REQ-9/REQ-6 so consequential weighed agreements are recorded where decisions are recorded, or add a non-goal explicitly declining the trail with rationale.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:110-123
  P: The judgment ACs (AC-7..9) define scenario shapes but no N (number of scenarios) and no pass bar (all-pass? majority?), and no grading rubric for the 'fresh agent' grader. The honest-limit paragraph references 'N scenarios' with N unbound. Deferring exact commands to TASKS is fine; deferring the acceptance threshold makes the criteria unrejectable, against spec-driven-development's bar.
  F: Set N per AC (even N=1 each, honestly labeled) and the pass rule (e.g. all scenarios must pass; a failure blocks the gate) in SPECS, leaving only commands to TASKS.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:57-62 vs :106-108,113-115
  P: REQ-8's requirement that reflexive opener tokens ('you're right,' 'fair,' 'good pushback') be named as the anchoring reflex is not guaranteed by any AC: AC-6's '≥2 new red-flag thoughts covering both polarities' can be satisfied without any opener-token red flag, and AC-7's behavioral check does not inspect the opening tokens of the response.
  F: Add to AC-6: at least one new red flag or anti-pattern names the reflexive concession opener; optionally have AC-7's grader check the stance is not voiced before the weighing in token order.

====================== designFlaw ======================
VERDICT: needs-fixes — the acceptance suite (AC-7..9) is satisfiable by a weigh-then-always-agree sycophant, the exact "ritual alternative-listing / sycophancy with more tokens" failure the BRIEF amendment declares disqualifying; the sycophantic polarity is never behaviorally tested while the contrarian polarity is.
[BLOCKER] design-chain/eod-weigh-before-stance/SPECS.md:110-119 (AC-7..AC-9)
  P: Polarity-flip test fails at the acceptance layer: an agent that always emits a visible weighing and then always agrees passes AC-7 (presence-only: 'weigh an alternative before taking a stance'), AC-8 (silent on trivial), and AC-9 (agrees when user is right). There is no scenario where the user's direction is wrong and compliance = weighed pushback, and no AC exercises REQ-7's anti-theater guard. The suite therefore cannot reject the exact failure the BRIEF amendment forbids ('ritual alternative-listing — sycophancy with more tokens'), violating spec-driven-development's bar that acceptance criteria be rejectable and REQ-2's own 'must not be satisfiable by flipping polarity' claim (the contrarian polarity is tested by AC-9; the sycophantic polarity — the motivating failure — is never tested).
  F: Add a fourth judgment AC: a scenario with a measurably weaker user-proposed direction where the compliant output is disagreement after weighing; and instruct AC-7's grader to fail strawman/ritual weighing using REQ-7's discriminator (alternative weighed must be the strongest one, with a real cost stated).
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:38-42 (REQ-5)
  P: The triviality floor is on the wrong axis: it keys on work size (AGENTS.md:26 'anything beyond a one-file, fully-understood change', with 'when in doubt, treat it as non-trivial' biasing toward firing), not decision stakes. Arguable-but-inconsequential choices inside non-trivial work (flag names, section ordering) get full REQ-3 generative ceremony — REQ-4's escape requires genuine dominance and does not cover 'arguable but bikeshed'. This reproduces the tedious-contrarian over-fire RESEARCH §6 Q2 warned about. AC-8's scratch-file exemplar only passes if triviality attaches to the proposal, but REQ-5's text attaches it to the work ('directions that shape non-trivial work'). The cited precedent (decision-memory rule 7) actually keys on consequence ('only when it matters'), not size.
  F: Define the floor on the direction's consequence — hard-to-reverse or shapes subsequent work — matching the decision-memory rule 7 precedent REQ-5 itself cites, so low-stakes choices inside non-trivial work are exempt.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:11-18 (REQ-1) vs :44-49 (REQ-6)
  P: Trigger granularity is unspecified: REQ-1's condition 'without having independently weighed alternatives' does not say whether a prior weighing discharges the duty for subsequent related proposals. Read per-proposal, every child proposal of a settled decision re-fires the ceremony — a compliant relitigation channel contradicting REQ-6's 'no relitigating survives in force'. Read per-decision-space, one early weighing licenses anchor-and-agree for the rest of the session — the sycophancy loophole REQ-2 forbids. Both readings satisfy the text and produce opposite behaviors; it also leaves the per-proposal frequency unbounded (a session of ten correct proposals compels ten weighing sentences, since rule 5's nagging guard covers only re-raising the same decision).
  F: State the discharge condition: the trigger fires per un-weighed decision and is discharged once the weighing is voiced; a settled decision suppresses re-weighing of its sub-proposals absent new information, mirroring rule 5's 'new evidence' reopen clause.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:44-49 (REQ-6, 'residual' clause)
  P: 'the new trigger covers the residual where the disagreement-space is trade-offs, not facts' admits a partition reading: any proposal embedding a checkable premise is case-(a) territory, so check-then-adopt satisfies the spec with no weighing — while REQ-1's plain text says the trigger fires on any adopt-without-weighing. Nearly every real proposal embeds some checkable premise (including the motivating incident's 'adopt the methodology first'), so the partition reading swallows the rule; the two REQs conflict.
  F: State explicitly that the premise check discharges only case (a): if, after the check, alternatives remain unweighed and the triviality floor is met, case (c) still fires.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:57-62 (REQ-8)
  P: Enumerating banned opener tokens ('you're right,' 'fair,' 'good pushback') adopts the mitigation shape the spec's own evidence says fails: RESEARCH §4 documents Claude 4's blunt 'never open with praise' ban being replaced by calibrated language and ELEPHANT's 'be less validating' overcorrecting. 'Reflexive' is unobservable at generation time, so agents will avoid the tokens wholesale — including earned concessions under surviving rule 4 ('concede on evidence') when the human is plainly right (natural output in AC-9 and case-(b) concessions). The rationale also over-claims: in case (c) the user's lean is absorbed at reading time, so output-token order is cosmetic per Turpin — which REQ-9 itself concedes.
  F: Name the reflex behaviorally (frame conceded before any weighing has been voiced or referenced) instead of listing tokens, and state that a weighing voiced in prior turns counts as preceding, so an earned concession-opener is compliant.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:11-18, 78-83, 97-99 (REQ-1/REQ-11/AC-2)
  P: The routing surface fires with the wrong verb. The house description grammar (RESEARCH §3) has one compressed-imperative slot, currently 'challenge once with evidence before complying' — inapplicable to case (c) by the BRIEF's own definition (no evidence in hand; trade-offs, not facts). REQ-1 mandates the trigger phrase in the description, but nothing requires the imperative to cover weigh-before-stance, and AC-2 checks only the 'Use when' prefix, a trigger phrase, and the tail. On the Claude skills surface — the one whose 'routing failure' rationale REQ-1 rests on — the description is all the router sees, so case (c) can route into a misdirecting instruction and still pass every AC. (The cursor/copilot/AGENTS.md surfaces are covered by REQ-10/AC-3; Gemini routes transitively via AGENTS.md:86-88.)
  F: Extend REQ-11 and AC-2: the description's imperative clause must also name the weigh-before-stance behavior (e.g., contain a 'weigh ... before' phrase), not just the new trigger.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:28-36 (REQ-3 vs REQ-4)
  P: When the user's own framing already spans the real option space ('should we do A or B?'), REQ-3's 'not merely score the human's option against a runner-up the human happened to mention' devalues weighing B, while generating a third option is REQ-4's manufactured rival — the letter of the two REQs excludes the compliant behavior. The 'duty to look' sentence implies the reconciliation, but DESIGN could encode the wrong reading.
  F: State explicitly that a user-mentioned alternative which survives the agent's independent look is a valid weighing target; the duty is the look, not novelty of the option.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:11-18 (REQ-1) vs AGENTS.md:9-15
  P: No carve-out for tier-1 instruction priority: case-(c) proposals are often explicit instructions ('do X first'), where AGENTS.md makes noting the trade-off optional ('you may note') and the user's explicit instruction always wins. The spec makes a voiced weighing mandatory even against an explicit 'skip the analysis, just do it' — on weaker ground than the existing challenge-once duty, which rests on contrary evidence in hand. The '; their decision still wins.' tail covers post-hearing compliance, not a pre-hearing opt-out.
  F: Scope the duty: an explicit user instruction to proceed without deliberation discharges the weighing requirement (per AGENTS.md instruction priority), leaving the trigger for un-instructed proposals and invitations to weigh in.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:127-130 (Non-goals, H1 lock)
  P: Forbidding any H1 change leaves a third of the extended trigger space (weigh-before-stance with no evidence in hand) undescribed by the skill's own headline imperative 'Challenge Once, With Evidence, Before Complying' — the extended skill's title misdescribes its new duty.
  F: Permit DESIGN to propose an H1 spanning both duties, or record the title/scope mismatch as an accepted debt in the decision log.

====================== testability ======================
VERDICT: needs-fixes — REQ-6 (existing guards survive verbatim) has no acceptance criterion at all, so the gate cannot reject an edit that breaks the very guardrails the brief and the governing principles say must survive.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:44-49 vs :92-123
  P: Traceability hole on a named guardrail: REQ-6 demands challenge-once / decision-wins / no-relitigating / one-clear-statement / rule-1 survive "verbatim in force," but no AC (machine or judgment) verifies it. "Verbatim" is trivially machine-checkable (grep the exact rule-3/4/5 phrases in skills/evidence-over-deference/SKILL.md, or assert the diff leaves those lines untouched), yet AC-1..6 count only additions and AC-7..9 never exercise a settled-decision scenario. A diff that guts rule 5 passes every listed AC — violating governing principle 3 undetected.
  F: Add a machine AC: grep for the exact surviving phrases (e.g. 'Challenge once, with evidence', 'The human's decision wins after being heard', 'No relitigating', 'One clear statement, not a filibuster') in SKILL.md post-edit, and/or assert the diff contains no deletions/modifications within the existing rules 1-5 lines.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:110-123
  P: AC-7..9 are not specified tightly enough to run or to fail: no scenario count (the honest-limit line says "N scenarios" with N never bound), no pass bar (1/1? all-pass? majority?), no re-roll policy (nothing forbids the author re-running until a fresh agent complies once — exactly the theater REQ-9 warns about), no grader rubric or prompt shape, no definition of "fresh agent" (model? context? blind to authorship?), and no required evidence artifact (RESEARCH's own probes were recorded in experiments/gate-behavior/decisions.jsonl; these evals leave no mandated trail). Two reviewers running these ACs can reach opposite verdicts, so they are not acceptance criteria a reviewer can reject (governing principle 1).
  F: Pin in SPECS: N scenarios per AC (e.g. ≥3), all-must-pass on a single disclosed attempt, re-runs reported not silently discarded, grader = agent with no session history given a fixed rubric derived from REQ-3/4/8, and transcripts+verdicts committed under a named path as the evidence artifact.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:113-115
  P: AC-7 is internally contradictory and skips REQ-1's own rationale: it is labeled "(fires)" but its mechanism is "the extended skill text, given to a fresh agent" — injection, not routing. REQ-1's stated rationale is that the original failure was a ROUTING failure (the description must route invocation). No AC tests that a case-(c) prompt causes the skill to be selected via its description; injecting the text pre-empts the only thing REQ-1 says mattered.
  F: Split AC-7 into AC-7a (routing: headless run with the pack installed and skills routed by description; assert the skill is invoked on a case-(c) scenario) and AC-7b (compliance given the text). If routing cannot be tested in the harness, say so explicitly as a stated limit instead of labeling an injection test "fires".
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:113-115 vs :28-32,:51-55
  P: AC-7's pass condition ("weigh an alternative before taking a stance") omits everything REQ-3 and REQ-7 add: the alternative must be agent-GENERATED (not the user's runner-up), the STRONGEST one, with its real cost stated. A transcript that names a strawman and waves it away — the BRIEF amendment's ritual-alternative-listing, "sycophancy with more tokens" — passes AC-7 as written. The anti-theater requirement is checked only as a static bullet's existence (AC-6), never behaviorally.
  F: Tighten AC-7's grading target: pass requires (i) an alternative not mentioned by the user, (ii) a stated cost/trade-off of that alternative, (iii) stance voiced after the weighing; grader explicitly instructed to fail strawman-and-dismiss transcripts.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:97-101
  P: The pass predicates of two "machine-checkable" ACs are delegated to the implementer: AC-2's middle conjunct ("contains a proposes-a-direction trigger phrase") has no pattern at all — the only exact greps are `^description: Use when ` and the `; their decision still wins.` tail — and AC-3 explicitly defers the grep phrases to DESIGN. The same chain that writes the prose picks the strings the gate greps for; DESIGN could choose a phrase already present (e.g. 'premise') and pass vacuously. A SPECS reviewer cannot reject these ACs because their content doesn't exist yet (governing principle 1: ACs a reviewer can reject).
  F: Pin minimal semantic tokens in SPECS while leaving exact prose to DESIGN: e.g. AC-2 description must match /propos|direction|framing/ AND /weigh/; AC-3 each of the four files must contain both a 'propose/direction' token and a 'weigh before stance' token, exact phrasing free.
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:104-105 vs :70-76
  P: AC-5's file set is not mechanically derivable and rests on a literal contradiction. "The six files of REQ-10/REQ-11": REQ-10 enumerates FIVE files (AGENTS.md, README.md, cursor .mdc, copilot instructions, honest-reframing SKILL.md — I counted); REQ-11 enumerates zero (it constrains SKILL.md's content without naming it as a file). Six is reachable only by inference. Worse, REQ-10 closes with "No other file changes" — which, read literally, forbids editing skills/evidence-over-deference/SKILL.md, the primary target, since it is absent from REQ-10's list. A DoD script implementing AC-5 has no authoritative list to diff against.
  F: Enumerate all six absolute repo paths in AC-5 itself (including skills/evidence-over-deference/SKILL.md) and reword REQ-10's closer to "no file changes beyond SKILL.md plus this set."
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:104-105
  P: AC-5's "the diff" has no defined baseline — diff of what against what? design-chain/ is currently untracked (`git status` shows `?? design-chain/`) and spec-driven-development rule 4 requires the chain docs to land as versioned artifacts; the spec's own decision log also says review verdicts get appended to SPECS.md after review. Under a PR-diff or branch-diff reading, AC-5 fails by construction the moment the chain docs are committed; under a per-commit reading it passes — the AC's verdict depends entirely on an unstated choice.
  F: Define the baseline: "AC-5 applies to the single implementation commit named in TASKS (git show --name-only <sha> lists exactly the six files); design-chain doc commits are separate and exempt."
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:11-18 vs :97-99,:106-108
  P: REQ-1's body half has no AC. REQ-1 requires the trigger in BOTH the description AND the `## When to use` body, with a concrete example in the parenthetical-self-question style. AC-2 checks only the description; AC-6 counts red-flag bullets and a new rule but never the When-to-use trigger sentence or its parenthetical example. An edit adding the rule + red flags but no body trigger passes AC-1..6 while violating REQ-1 — whose own rationale says asymmetric coverage reproduces the original failure.
  F: Add to AC-6 (or a new AC): the `## When to use` section's diff must add a sentence containing the trigger tokens and at least one new parenthetical question, checkable by grepping the added lines of that section's hunk.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:110-111
  P: "phase 6" is undefined anywhere in the repo: spec-driven-development names exactly five phases (BRIEF→RESEARCH→SPECS→DESIGN→TASKS) and grep finds no 'phase 6' or 'skill-trigger evals' convention in AGENTS.md, CONTRIBUTING.md, or skills/. The DoD gate cannot locate the stage that runs AC-7..9.
  F: Replace "phase 6" with an explicit definition: "a post-TASKS eval step defined in TASKS, executed before the implementation commit merges," or name it relative to the five-phase chain.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:116-117 vs :34-36
  P: AC-8's pass condition ("no weighing ceremony") is undefined relative to REQ-4's dominant-option escape, which blesses a one-sentence weighing. If the fresh agent emits one sentence of weighing on the trivial scratch-file proposal, the grader has no rule saying whether that passes or fails — the AC-8/REQ-4 boundary is exactly where graders will disagree.
  F: Define the bar: on a sub-floor trivial proposal, zero weighing sentences is the pass; any alternatives-considered prose is a fail (the REQ-4 escape applies only above the REQ-5 floor).
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:106-108
  P: AC-6 sits in the "machine-checkable" list but half its conjuncts are semantic judgments: "covering both polarities" and "(paired-polarity + ritual-listing)" cannot be grepped; only the counts (≥2 bullets, one rule, two section hunks) can. A DoD script can verify quantity while polarity coverage silently fails.
  F: Split AC-6: keep the counts as the machine AC; move the polarity/ritual semantic checks into the judgment list (or pin grep-able tokens for them in DESIGN as AC-3 does).
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:96 vs :78-83
  P: AC-1 (`pre-commit run --all-files`) does not execute commit-msg-stage hooks — conventional-pre-commit and check-commit-trailer are `stages: [commit-msg]` in .pre-commit-config.yaml — so REQ-11's commit-message constraints (feat(...) prefix, provenance trailer) are covered by no AC. They are backstopped by commit-time hooks and CI replay, but the spec's declared DoD list misses them, so a DoD script consuming only AC-1..6 would report REQ-11 fully green without ever checking the message.
  F: Add an AC naming the actual check (e.g. run the commit-msg hooks against the implementation commit's message: `pre-commit run --hook-stage commit-msg --commit-msg-filename <msgfile>`), or explicitly note REQ-11's commit half is enforced by the commit-time/CI gate rather than AC-1.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:57-68,:85-90
  P: Three more REQs have existence-only or zero AC coverage: REQ-8's requirement to NAME the reflexive openers ('you're right,' 'fair,' 'good pushback') as the anchoring reflex has no textual AC; REQ-9's content constraints ('almost nothing' survives; post-hoc-rationalization note added) are reduced by AC-6 to 'an Enforcement addition' of any content; REQ-12's constraints (the specific incident, anonymized, every new rule carries its field finding) are reduced to 'an In-practice addition' of any content (anonymization is at least backstopped by the check-no-private-identifiers hook via AC-1).
  F: Add cheap grep ACs: Enforcement section still contains 'Almost nothing' and gains a rationalization token; When-to-use/red-flags diff contains at least one quoted opener token; In-practice hunk contains a revive-vs-rebuild/triage token.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:94,:110 vs skills/definition-of-done-tooling
  P: The spec never says how the DoD phase consumes AC-7..9. The pack's own definition-of-done-tooling rule is that every `required` criterion is backed by a real runnable check and the author never self-certifies — but REQ-9/non-goals forbid tooling, so AC-7..9 can never be runnable checks, and the spec doesn't declare their gate treatment (required-with-recorded-judgment-verdict? n/a-with-visible-decision?). Per CONTRIBUTING.md's bar ('pass the same checks the pack preaches'), leaving this mapping undefined lets the author self-certify the judgment ACs.
  F: State in SPECS how each AC maps into the done-gate: AC-1..6 as runnable checks (commands in TASKS), AC-7..9 as judgment criteria whose recorded verdict must come from the non-author grader, with the transcript artifact as the evidence the gate points to.
