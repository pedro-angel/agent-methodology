====================== factual ======================
VERDICT: needs-fixes — AC-6's Why-section calibration token (`calibrat`) already exists in the baseline `## Why` paragraph (SKILL.md:33 "can calibrate on"), the very line REQ-14 orders extended, so the pinned token cannot reject a Why edit that omits the calibration consequence — the round-2 MAJOR this pin claims to fix is silently reopened; all six round-2 factual findings are otherwise genuinely resolved and every other new v3 claim verifies against the repo
[MAJOR] design-chain/eod-weigh-before-stance/SPECS.md:205-206 (AC-6 Why conjunct) vs skills/evidence-over-deference/SKILL.md:33 and decision log :354
  P: The pinned Why token is vacuous: AC-6 requires the `## Why` added text to contain `information` OR `calibrat`, but the baseline Why paragraph (SKILL.md:33) already contains "calibrate" ("a signal they can calibrate on") — verified by grep — and it sits in the same paragraph as the "agreement means something" line REQ-14 explicitly orders extended. Under AC-6's own section-scoped-diff definition, any modification of that paragraph puts the pre-existing "calibrate" into the ADDED text, so a Why edit lacking the calibration consequence passes the machine check; no other AC or listed spec-compliance-review duty covers REQ-14's Why half (the DoD mapping routes only AC-6's polarity/theater semantics to the review). The decision log's claim that the round-2 MAJOR was addressed by "Why calibration-token pinned" is therefore factually not achieved — unlike AC-2/AC-3, whose non-vacuity claims verify (current description fails both new regexes; \bframing\b does not match AGENTS.md:88's "reframing"; all four paraphrase passages are currently token-free).
  F: Pin `information` alone (verified absent from the baseline Why section) or require the calibration token to appear in a sentence not present in HEAD's Why section; alternatively add REQ-14's Why semantics to the spec-compliance review's enumerated duties in the DoD mapping.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:197-198 (AC-6 new-rule conjunct) vs skills/evidence-over-deference/SKILL.md:29
  P: Same vacuity class, weaker path: baseline rule 5 already contains both "settled" and "*new* evidence" (verified by grep), and AC-7 explicitly allows renumbering — inserting the new rule before rule 5 renumbers it, making old rule 5's full line appear as ADDED text in the rules-section diff, so a naive implementation finds the settled/new-evidence token without the genuinely new rule carrying any REQ-13 discharge language. Second-order because S7 covers REQ-13 behaviorally and a content-aware diff avoids it, but the token pin's rejectability premise is false as stated.
  F: Require the settled/new-evidence token within the new rule's own text, defining added text to exclude lines present verbatim in HEAD modulo the leading rule number.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:320 (traceability row 6c) vs :297-307 (DoD mapping)
  P: The 6c row cites "spec-compliance checklist (narrow wording)" as half its coverage, but that checklist item is defined nowhere outside the table cell: the DoD mapping enumerates the spec-compliance review's recorded duties as AC-6's polarity/theater semantics only, so a DoD consumer following the mapping never learns to check REQ-6c's narrow rendering (explicit waiver, not imperative mood). AC-16/S8 exists and covers the behavioral half, so the row is not false — the checklist pointer is just unanchored.
  F: Add REQ-6c's narrow-wording check to the DoD mapping's enumeration of the spec-compliance review's recorded duties.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md — round-2 factual resolution record (verification, no action needed)
  P: All six round-2 factual findings verified genuinely resolved in the v3 text: REQ-6 traceability split into 6a|AC-7, 6b|AC-12, 6c|AC-16+checklist with spurious AC-8 removed and S8 now existing (:278-281, :318-320); REQ-4 row now lists AC-11 (neg), AC-13 (neg), AC-14, AC-17, accurate against v3's S3 constructed-C and S9 empty-space designs (:316); round-1 testability tally corrected to 8 MAJOR and re-counted true against the ledger (:348); REQ-15 now cites the git-log probe and round-1 completeness ledger, and the probe re-run confirms no chain doc in history (:159-161); the extended-superpowers phase label is gone, runner defined functionally (:238-239); the Claude routing basis is stated inline and correctly attributed to the round-1 design-flaw ledger (:21-25). Also re-verified: all seven AC-7 guard phrases exact-match current SKILL.md with straight apostrophes; 21 skills (AC-18); decision-memory rule 7 "only when it matters"; no markdown diff driver in .gitattributes; conventional-pre-commit and check-commit-trailer at commit-msg stage; RESEARCH §4 (ELEPHANT, SycEval, Claude 4 praise-ban replacement) and §5 R0(1)/R2-off-criterion cites; BRIEF:71 quote and BRIEF:118 weak lean; all round-2 tallies in the decision log match the ledger.
  F: None — recorded so the round-3 ledger shows the verification was performed, not assumed.

====================== completeness ======================
VERDICT: clean with MINORs — all seven round-2 completeness findings genuinely resolved in v3 (verified against SPECS text, the four sync-surface files, and SKILL.md); no BLOCKER/MAJOR remains
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:55-64 (REQ-5 tier b) vs :261-269 (AC-13/AC-14) and traceability row 5
  P: The doubt tier v3 made explicit — 'stakes genuinely in doubt: treated as above-floor, the one-sentence form — doubt buys a sentence, never a filibuster and never silence' — has no rejector: S5 is pinned unambiguously tier-(a), S6 is dominant tier-(c), no scenario stipulates in-doubt stakes, and AC-6 pins no floor/doubt token, so an implementation that omits the doubt tier or inverts it (doubt -> silence, the under-fire direction) passes every machine AC and all of S1-S9.
  F: Pin a doubt/consequence token in AC-6's new-rule check (cheapest), or add a tier-(b) stipulation to an existing scenario (a proposal whose stakes are plausibly but not clearly above the floor; pass = one-sentence weighing, fail = silence or full ceremony).
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:320 (traceability row 6c) vs :297-307 (DoD mapping)
  P: Row 6c claims coverage by 'spec-compliance checklist (narrow wording)', but no checklist is enumerated anywhere in the spec: the DoD mapping defines the implementation-phase spec-compliance review's recorded verdict as covering only AC-6's polarity/theater semantic halves, so a DoD consumer following the mapping records a complete-looking review verdict without ever checking REQ-6c's narrow rendering — the over-broad reading (any imperative-mood proposal treated as a waiver) hollows the trigger in the field and S8 only rejects the opposite (over-fire) direction.
  F: Add the 6c narrow-wording item to the DoD mapping's description of what the spec-compliance review verdict must cover (one clause: 'review confirms the discharge is rendered as an explicit deliberation waiver, not imperative mood'), so the checklist the traceability row cites exists in the protocol text.

====================== designFlaw ======================
VERDICT: clean — all seven round-2 design-flaw findings verified genuinely resolved in the v3 text (S3 constructed-C kills the magic-sentence bypass, the re-run rule yields one decidable verdict on a fail-then-pass walk, the three-tier floor, REQ-8/AC-9 fusion, S7 stipulation+re-fire, and S8/REQ-6c coverage all check out); only two boundary-stipulation MINORs remain (S8 premise-free construction, S9 examined-candidates vs invented-rival), neither gate-blocking
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:278-281 (AC-16/S8) vs :69-81 (REQ-6a/6b/6c)
  P: S8 is the only boundary scenario without a construction stipulation. REQ-6c's waiver narrowly discharges only 'the voiced weighing', while REQ-6a/6b keep rule 1's premise-check and rule 2's evidence-based challenge alive (skills/evidence-over-deference/SKILL.md:25-26). If TASKS constructs S8's consequential direction with an embedded checkable premise or contrary evidence in hand, the spec-correct output includes a premise check / one-sentence challenge — which AC-16's fail condition ('Fail: weighing anyway') plus its REQ-6c-only rubric invites the grader to fail. The gate could fail a good implementation on a badly-built S8; v3 stipulated construction for S3/S5/S7/S9 but not S8.
  F: Add a construction stipulation to AC-16 parallel to S5/S7's: S8's direction is constructed premise-free and evidence-neutral (the waived deliberation is the only live duty), or add REQ-6b to S8's rubric with a note that a surviving premise check is not 'weighing anyway'.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md:282-285 (AC-17/S9) vs :44-53 (REQ-4) and :83-87 (REQ-7)
  P: Thin grader boundary inside S9 itself: a good implementation's honest 'where the look went' naturally names examined-and-rejected weak candidates ('I checked forking and pinning — neither is real because X; nothing else exists'), which a strict reading of the fail condition ('an invented rival weighed for show') could score as a manufactured rival. REQ-4/REQ-7's rubric sources point the right way (strawman = presented-as-serious to perform balance), but the pass/fail texts as written let two graders split on the same compliant transcript.
  F: Stipulate in AC-17 that naming examined non-serious candidates as part of the empty-look statement is compliant; the fail condition targets a rival presented as serious and ceremonially weighed to perform balance.

====================== testability ======================
VERDICT: clean — all 4 round-2 testability MAJORs and 7 MINORs verifiably resolved in v3 (pinned regexes executed against current repo files: AC-2 anchored clause regex and AC-3 word-bounded/passage-anchored tokens now reject both round-2 vacuous cases; re-run rule decidable; AC-6 section-scoped diff implementable; AC-8 both halves runnable; DoD AC-6 split unambiguous); 3 residual MINORs, none gate-breaking
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md AC-2 (lines 168-176)
  P: The imperative-clause regex `—[^;]*weigh[a-z]*[^;]*before` assumes exactly one em dash in the description. Demonstrated leak: a trigger clause containing an em-dash appositive with a weigh trigger-noun ('Use when the human proposes a direction — a framing you haven't weighed — challenge once with evidence before complying; their decision still wins.') matches the regex (verified by execution) while containing no weigh verb in the imperative clause — the exact trigger-noun-only failure REQ-11 forbids. Current description has one em dash and fails the regex as intended, so this needs a specific authoring shape to trigger.
  F: Exclude em dashes from the gap classes (`—[^—;]*weigh[a-z]*[^—;]*before`, anchoring to the last em dash) or add 'exactly one em dash' to AC-2's physical-line constraints.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md AC-3 (lines 176-183)
  P: The guard-retention disjunct '`decision` near `wins`/`heard`' leaves 'near' unquantified — AC-6 quantifies its own proximity clause ('within two lines') but AC-3 does not. README.md:34 is the only passage that relies on this disjunct today (no relitigat/concede token in the row), so two script authors could implement different proximity windows for the same pass predicate.
  F: Quantify: e.g. both tokens within the anchored passage (single row/bullet) or within N words, matching AC-6's explicit-proximity style.
[MINOR] design-chain/eod-weigh-before-stance/SPECS.md AC-6 Enforcement clause (lines 207-209)
  P: Every AC-6 token is pinned as a backticked literal except one: 'a rationalization token' has no pinned stem, so a strict `rationaliz` grep would fail a good implementation that renders REQ-9a as 'post-hoc justification' — the only remaining unpinned-token instance of the delegated-grep pattern v3 otherwise eliminated. Risk is small because REQ-9a's own phrasing is 'post-hoc rationalization'.
  F: Pin the literal in the same style as the others: e.g. contains `rationaliz` or `post-hoc`.
