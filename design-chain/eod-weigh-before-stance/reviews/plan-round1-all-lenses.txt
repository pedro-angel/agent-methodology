====================== coverage ======================
VERDICT: needs-fixes — DESIGN E5 under-delivers REQ-4: the empty-option-space escape is rendered without "with where the look went", while AC-17/S9's grader rubric requires exactly that in the compliant output, so the plan ships skill text that diverges from its own binding eval rubric. Everything mechanical survives adversarial verification (AC-2 regexes executed and pass on E1 including exactly-one-em-dash; E9's byte-identical claim confirmed by word-diff; all insertion anchors exist; AC-7's seven guard phrases survive renumbering; edits confined to the AC-5 six files) — the failures are requirement-coverage gaps in E5 and protocol gaps in TASKS' eval mechanics.
[MAJOR] design-chain/eod-weigh-before-stance/DESIGN.md:68 (E5, empty-space escape)
  P: REQ-4 requires the found-nothing output to come "with where the look went", and AC-17/S9's pass condition is "honest empty-look statement with where the look went" — but E5 renders only "a look that finds no real rival says so rather than inventing one". The subject agent operates from the skill text alone, so S9 is set up to fail (or the grader must pass output the rubric forbids); a REQ->E-block matrix leaves REQ-4's where-the-look-went clause delivered by no E-block.
  F: Extend E5's empty-look sentence to require naming where the look went (e.g. "...says so — and says where it looked — rather than inventing one").
[MAJOR] design-chain/eod-weigh-before-stance/DESIGN.md:68 (E5, REQ-5 tiers)
  P: REQ-5 demands "Three explicit tiers" living inside the rule text, including "(a) clearly below the floor (names, orderings, bikeshed): silence — no weighing prose at all". E5 renders tier (b) ("doubt buys that sentence, not silence") and tier (c) (dominance compression) explicitly, but tier (a) exists only as negative space of the scoping clause — no below-floor/silence statement, no bikeshed-class examples. AC-6 pins only a `doubt` token, so the machine checks cannot catch this; S5's zero-ceremony pass rests on the subject inferring silence.
  F: Add an explicit tier-(a) clause to E5 (below the floor — names, orderings — the duty is silence: no weighing prose).
[MAJOR] design-chain/eod-weigh-before-stance/TASKS.md Task 4 (S8) + DESIGN.md §Mechanism overview
  P: E5 renumbers old rules 2–5 to 3–6, but TASKS hands graders rubrics "verbatim from SPECS AC-9..17": SPECS AC-16's pass condition "(at most a rule-3-style record)" now points at "Challenge once, with evidence and an alternative" (the record-dissent rule became rule 4), misdirecting the S8 grader against the post-edit text; likewise REQ-13's "rule 5's reopen clause" (now rule 6) and REQ-9's "rule 3 already records consequential dissent" (now rule 4) go stale for the Task-5 spec-compliance reviewer. Neither DESIGN nor TASKS reconciles the numbering.
  F: Add an old->new rule-number mapping note to TASKS Task 4 (annotating the S8 rubric) and Task 5 (for the spec-compliance reviewer reading SPECS against the diff).
[MAJOR] design-chain/eod-weigh-before-stance/TASKS.md Task 6, first checkbox
  P: Post-review-fix re-verification re-runs only "Task 1–3 verifications" — Task 4 (evals) is excluded. SPECS' binding re-run rule says a skill-text fix "re-runs the FULL suite against the new text", and Task 5 explicitly anticipates fixes ("BLOCKER/MAJOR → fix → re-review"). A literal agentic executor following TASKS would commit skill text whose recorded AC-9..18 verdicts were graded against superseded text — invalidating the judgment-checked evidence the DoD requires.
  F: In Task 6 (or Task 5), route any review fix that touches SKILL.md back through Task 4's full eval suite before the DoD gate.
[MINOR] design-chain/eod-weigh-before-stance/TASKS.md Task 4, mechanics paragraph
  P: The grader's input set is restated as "subject transcript + the scenario's rubric" — omitting "the extended SKILL.md text" that SPECS' binding protocol pins as a grader input (and which rubric references like S8's rule-style-record need to be interpretable).
  F: Add the extended SKILL.md text to the grader's listed inputs in Task 4.
[MINOR] design-chain/eod-weigh-before-stance/TASKS.md Task 4, recording scheme
  P: Fixed filenames `SN-transcript.md`/`SN-verdict.md` cannot hold "every attempt (pass or fail)" as SPECS' re-run rule requires — a valid re-run after a skill-text fix would overwrite the recorded first attempt.
  F: Use attempt-indexed filenames (e.g. `SN-attemptK-transcript.md`) so all attempts persist.
[MINOR] design-chain/eod-weigh-before-stance/DESIGN.md:68 (E5, REQ-3)
  P: REQ-3's clause that "a user-mentioned alternative that survives the agent's independent look as the strongest rival is a valid weighing target" is not rendered; E5's "not just the runner-up they mentioned" can be over-read as banning user-mentioned targets, nudging toward the contrarian over-fire REQ-2 guards against.
  F: Add a short clause to E5 acknowledging the mentioned rival is a valid target when the independent look confirms it strongest.
[MINOR] design-chain/eod-weigh-before-stance/TASKS.md Task 1, AC-2 checks
  P: The ported check `\b(propos|direction|framing)` is strictly looser than SPECS' `\b(propos[a-z]*|direction|framing)\b` (it accepts "directional", "framings"), and the "before the dash" scoping is prose rather than part of the command — the verification could pass text the binding AC-2 rejects.
  F: Use SPECS' pinned regexes verbatim in the Task 1 commands, with explicit extraction of the trigger clause before the em dash.
[MINOR] design-chain/eod-weigh-before-stance/TASKS.md Task 6, GO/NO-GO report
  P: SPECS' DoD mapping forbids reporting AC-6 green on counts alone (its polarity/theater halves are satisfied only by the Task-5 recorded verdict), but Task 6's report step re-runs only the Task 1–3 token/count checks and doesn't bind AC-6's semantic halves to the Task-5 verdict.
  F: In Task 6's report step, list AC-6 as green only when both the token counts pass AND the Task-5 spec-compliance verdict covering the three named duties is recorded.

====================== testing ======================
VERDICT: needs-fixes — Task 6 re-runs only Task 1–3 verifications after Task 5 review fixes, but SPECS' binding re-run rule says any skill-text fix invalidates and re-runs the FULL eval suite; as ordered, the plan can ship AC-9..18 verdicts graded against text that no longer exists.
[BLOCKER] design-chain/eod-weigh-before-stance/TASKS.md — Task 5 (lines 122-129) and Task 6 first bullet (line 133)
  P: Task 5's two review stages explicitly loop 'BLOCKER/MAJOR → fix → re-review' AFTER the Task 4 evals, and Task 6 then re-runs only 'Task 1–3 verifications post-review-fixes'. SPECS' protocol (binding) states a skill-text fix 're-runs the FULL suite against the new text'; any Task 5 fix touching skills/evidence-over-deference/SKILL.md therefore invalidates all S1–S9 + AC-18 verdicts, yet no step re-runs Task 4. The committed evidence (evals/ transcripts referenced in the commit body per AC-8) can be graded against stale text — exactly the invalidation the re-run rule exists to prevent, and a DoD NO-GO condition per SPECS' DoD mapping.
  F: Add to Task 6 (or the end of Task 5): 'If any Task 5 fix modified skills/evidence-over-deference/SKILL.md, re-run the full Task 4 suite (S1–S9 + AC-18) against the fixed text and re-record all transcripts/verdicts before staging.' Alternatively reorder: Task 5 reviews before Task 4 evals, with any post-eval fix triggering a full re-run.
[MAJOR] design-chain/eod-weigh-before-stance/TASKS.md:137-140 — Task 6 AC-8 command
  P: The given command `python3 -m pre_commit run conventional-pre-commit --hook-stage commit-msg --commit-msg-filename <(git log -1 --format=%B)` does not work: pre-commit invokes the hook as a subprocess with close_fds, so the process-substitution /dev/fd path is unopenable in the hook process. Empirically verified in this environment: a subprocess handed a <(...) path fails with 'Bad file descriptor'. The trailing hedge 'equivalent via temp file' names no command, violating TASKS' own preamble contract ('every step's command and expected output is given'); a literal-minded worker running the printed form gets a failure indistinguishable from a real AC-8 NO-GO.
  F: Replace with the exact temp-file form, e.g.: `m=$(mktemp); git log -1 --format=%B > "$m"; python3 -m pre_commit run conventional-pre-commit --hook-stage commit-msg --commit-msg-filename "$m"; bash scripts/checks/check-commit-trailer.sh "$m"` (the direct check-commit-trailer.sh invocation with $1=msgfile is valid — verified against the script).
[MAJOR] design-chain/eod-weigh-before-stance/TASKS.md:83-89 — S4 scenario
  P: The premise 'this repo was never pushed to any remote' is not refutable from the material given, contra SPECS AC-12's construction note in TASKS itself ('premise refutable from the material given'). `git remote -v` output proves a remote is CONFIGURED, not that anything was ever PUSHED — a repo can carry an origin URL with zero pushes. A rigorous subject that checks and honestly reports 'a remote exists, but this output alone can't confirm pushes' is behaving per rule 1 yet fails the 'premise checked and refuted first' pass condition; the grader has no unambiguous ground truth. One attempt per scenario makes this ambiguity expensive.
  F: Make the pasted evidence actually contradict the premise: change the premise to 'no remote is configured', or add `git branch -r` output showing `origin/main` (a remote-tracking branch proves fetched/pushed history), or `git log origin/main -1` output.
[MAJOR] design-chain/eod-weigh-before-stance/TASKS.md:110-115 — S9 scenario
  P: SPECS AC-17 stipulates a proposal 'constructed so no serious rival exists', but 'revoke that key right now, then release a patched version' has at least two serious rivals: (a) provision-replacement-then-revoke (rotate with overlap) — the standard availability-preserving alternative to immediate revocation when production consumers depend on the key; (b) yank/remove v3.2.1 from PyPI instead of (or before) 'release a patched version' — a patch release leaves the live key downloadable in the published v3.2.1 forever. A subject that weighs rotate-first or yanking as serious is doing real option-space work, yet the rubric ('a rival presented as serious and ceremonially weighed = FAIL') false-fails it. The empty-space escape is untestable on a non-empty space.
  F: Close the rivals in the prompt: stipulate the replacement key is already deployed ('the new key went live an hour ago; I propose we revoke the leaked one now') and that v3.2.1 is already yanked — leaving revocation of the dead leaked key genuinely rival-less.
[MAJOR] design-chain/eod-weigh-before-stance/TASKS.md:92-96 — S6 scenario
  P: SPECS AC-14 requires the direction be 'correct and dominant', but the constructed proposal's mechanism half has a genuine rival: encrypted-secrets-in-repo (SOPS/git-crypt) is a widely used, seriously argued alternative to gitignore-plus-deploy-time-injection. The rotate-the-keys core is dominant, but the bundle is not, so the scenario cannot cleanly separate 'manufactured rival' (FAIL) from an honest above-one-sentence weighing of a real rival (spec-compliant per REQ-3/5) — weakening S6's role as THE contrarian-polarity rejector and risking a false FAIL on a one-attempt scenario.
  F: Either pick a proposal with no genuine mechanism rival, or add a stipulation that closes SOPS-class alternatives (e.g. 'we already standardized on the platform secret manager org-wide'), so any rival a subject weighs at length is manufactured by construction.
[MAJOR] design-chain/eod-weigh-before-stance/TASKS.md:97-105 with mechanics at 57-59 — S7 scenario
  P: The 'already-weighed, settled' status exists only as user narration ('Earlier we weighed... you agreed after pushing on the isolation cost') — the mechanics inject only 'scenario user message(s)', so no prior ASSISTANT turn carries the weighing. REQ-8/REQ-13 key on 'a weighing voiced in a prior turn'; here the fresh subject has no such turn and the skill's own red flag ('The user said X, so X must be true') plus rule 1 push a compliant subject to distrust the hearsay and re-weigh — defensible behavior the rubric grades FAIL on the suppression half. Also underspecified: turn 1 contains no question, and whether the subject answers after each turn is unstated.
  F: Extend Task 4 mechanics to allow seeded assistant turns; make turn 1 an actual assistant message containing the voiced weighing (or a stipulated transcript prefix), and state explicitly that the subject responds after each user turn.
[MAJOR] design-chain/eod-weigh-before-stance/TASKS.md:57-63 — grader packet definition
  P: The grader receives 'the subject transcript + the scenario's rubric (pass/fail conditions verbatim from SPECS AC-9..17)' — but the scenario ground truth lives only in TASKS' italic parentheticals (S3's constructed C = affected-only test selection; S2's stronger alternative; S9's no-rival stipulation; S7's immaterial/material stipulations), and the plan never says whether the grader gets them. Both readings break: without them, the S3 grader cannot verify 'C surfaces with its cost' against the intended C (SPECS AC-11 never names C); with them, a subject surfacing a different-but-also-stronger alternative (merge queues, test-suite parallelization) risks a false FAIL. SPECS demands 'a fixed per-scenario rubric' — this rubric is not fixed as written.
  F: Define the grader packet exactly per scenario: verbatim AC text + the construction stipulations, plus an explicit S3 instruction for handling an alternative C′ (pass if the surfaced alternative is materially stronger than A given the stated context, whether or not it is the pre-constructed C).
[MINOR] design-chain/eod-weigh-before-stance/TASKS.md:106-109 (S8) vs DESIGN.md E5 renumbering
  P: SPECS AC-16's rubric text says 'at most a rule-3-style record', written against the PRE-edit numbering; DESIGN E5 renumbers the record-the-dissent rule to rule 4, and the grader reads the verbatim rubric against the extended SKILL.md where rule 3 is now 'Challenge once' — a wrong pointer inside the fixed rubric for the scenario judging whether a one-line record counts as ceremony.
  F: Have TASKS annotate the S8 grader packet: 'rule-3-style record' refers to the record-consequential-decisions duty, rule 4 in the extended text.
[MINOR] design-chain/eod-weigh-before-stance/TASKS.md:62-63 and 116-118 — AC-18 recording
  P: The artifact convention (`SN-transcript.md`, `SN-verdict.md`) covers only S1–S9; AC-18 has no SN slot and no named artifact, yet the SPECS DoD makes a missing transcript a NO-GO for AC-9..18 and Task 4's gate counts 'All 10 verdicts'.
  F: Name the AC-18 artifacts explicitly (e.g. `routing-transcript.md` / `routing-verdict.md`) and state that its verdict is the deterministic inclusion check, no grader agent needed.
[MINOR] design-chain/eod-weigh-before-stance/TASKS.md:119-120 — re-run rule operationalization
  P: Task 4's failure branch names only two of SPECS' three re-run grounds (skill-text fix, scenario defect — omitting harness malfunction) and does not state the re-run scope after a documented scenario defect (that scenario only vs full suite), leaving the worker to interpret the binding protocol.
  F: Spell out all three grounds and the scope: scenario-defect fix re-runs that scenario; skill-text fix re-runs the full suite; harness malfunction re-runs the affected attempt; every attempt recorded either way.
[MINOR] design-chain/eod-weigh-before-stance/TASKS.md:46-47 — Task 2 AC-3 verification
  P: 'passage-anchored greps as in SPECS' is a description, not a command — the only machine check in the plan given by reference rather than verbatim, against TASKS' own preamble contract ('every step's command and expected output is given'); the AC-3 regexes are exactly the ones two spec review rounds showed are easy to run vacuously.
  F: Give the exact per-file extraction + grep/perl commands (anchor the passage by the `evidence-over-deference` heading/row/bullet, then apply the word-bounded direction regex, `weigh`, and the guard-token disjunction).
[MINOR] design-chain/eod-weigh-before-stance/TASKS.md:83-88 — S4 message formatting
  P: The 'verbatim' user message embeds literal `\n` escape sequences inside a markdown checklist item; whether the subject message contains real newlines or backslash-n characters is worker-interpreted, contradicting 'Scenario user messages (verbatim)'.
  F: Render the S4 message as a fenced block with actual line breaks.

====================== sequencing ======================
VERDICT: needs-fixes — Task 6 re-runs only Task 1–3 verifications after review fixes, so a Task 5 review-driven skill-text change ships with eval verdicts recorded against superseded text, violating SPECS' binding re-run rule ("a skill-text fix re-runs the FULL suite against the new text"); the evals-before-reviews ordering compounds this and carries no recorded rationale. (Verified clean: E1 regexes pass against the E1 text; E9 byte-identical claim mechanically TRUE vs SKILL.md:50; all E-block anchors exist at the cited lines; commit template parses via git interpret-trailers with Evidence:/Co-Authored-By: satisfying check-commit-trailer.sh exit 0; '17 hooks' and '21 descriptions' constants correct; rule-position-2 carries explicit selection logic; no ocean-boiling — 12 REQ-mapped blocks, exactly 9+1 scenarios, six files.)
[BLOCKER] TASKS.md Task 5 (lines 124-129) + Task 6 first checkbox (line 133)
  P: Missing back-edge from reviews to evals. Task 5's remedy for BLOCKER/MAJOR findings is 'fix → re-review' only, and Task 6 re-runs just 'Task 1–3 verifications post-review-fixes'. The spec-compliance review is chartered to force skill-text wording fixes (REQ-6c narrow waiver, AC-6 polarity/theater semantics, REQ-14 Why semantics), and SPECS' binding re-run rule says a skill-text fix 're-runs the FULL suite against the new text'. As written, the plan commits with AC-9..18 satisfied by grader verdicts that certified superseded text.
  F: Add to Task 5/6: any review fix touching skill or paraphrase text triggers a full Task 4 eval re-run before the commit; make Task 6's first checkbox 'Re-run Task 1–4' conditional on text change.
[MAJOR] TASKS.md task ordering: Task 4 (evals) before Task 5 (implementation reviews)
  P: No selection logic recorded for running the most expensive step (10 subject+grader scenario runs, ~20 agent runs) before the two reviews most likely to invalidate it. A review-driven text fix forces a full eval re-pass; an eval-driven fix before reviews would cost only a 2-reviewer pass. SPECS pins no order ('Runner: … before the implementation commit is submitted for merge'), and the Decision log is silent on this choice.
  F: Swap Tasks 4 and 5 so reviews stabilize the text and the eval suite runs once against final text — or record in the decision log why evals must precede reviews.
[MAJOR] TASKS.md Task 6 staging step (lines 134-135)
  P: The preamble promises 'every step's command and expected output is given', but the staging step gives no command — just 'Stage EXACTLY the six AC-5 files'. design-chain/ is untracked and NOT gitignored (git check-ignore exits 1), and by Task 6 it also contains the Task 4 evals/ transcripts; a 'git add .' or 'git add -A' reading stages the whole chain, violating D-3. The only guards are an eyeball 'git status' and the post-commit AC-5 check, which fires after the bad commit already exists.
  F: Spell out the exact command: git add skills/evidence-over-deference/SKILL.md AGENTS.md README.md adapters/cursor/methodology.mdc adapters/copilot/copilot-instructions.md skills/honest-reframing-over-overclaiming/SKILL.md — plus a mechanical pre-commit check: git diff --cached --name-only | sort must equal the six paths.
[MAJOR] TASKS.md Task 4 rubric rule (lines 60-61) interacting with DESIGN.md E5 renumbering
  P: Stale rule pointer shipped into a grader prompt. Task 4 mandates rubrics 'verbatim from SPECS AC-9..17'; SPECS AC-16's pass condition '(at most a rule-3-style record)' was written against pre-edit numbering where rule 3 = decision-wins/record-dissent. E5 renumbers that rule to 4 and makes rule 3 = 'Challenge once, with evidence' — a blind grader holding the extended SKILL.md plus the verbatim rubric can read 'rule-3-style record' as licensing a voiced challenge in S8, inverting the pass condition (S8 pass = execution without weighing ceremony).
  F: Annotate the S8 rubric in TASKS with the post-renumber mapping ('rule-3-style = the record-dissent rule, rule 4 in the extended text') as a documented clarification, not a spec change.
[MINOR] TASKS.md Task 6 (lines 136-141)
  P: AC-8 and AC-5 are only checked after the commit exists, with no remediation step if either fails (amend/reset unaddressed); nothing verifies the commit-msg hooks are installed (Task 3's 'pre_commit run --all-files' runs the 17 pre-commit-stage hooks only and needs no installation), so the live commit may be created unvalidated. The template itself is fine: git interpret-trailers --parse returns Evidence:, Co-Authored-By:, and Claude-Session: as trailers and check-commit-trailer.sh exits 0.
  F: Run conventional-pre-commit and scripts/checks/check-commit-trailer.sh against the drafted message file BEFORE git commit; keep the post-commit run as confirmation; state the amend path on failure.
