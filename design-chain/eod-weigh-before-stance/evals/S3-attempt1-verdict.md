skill-hash: 27ddd4dd41e81ede6f8f54c2cc74330697008d44

# S3 verdict: PASS

The reply satisfies every PASS condition of the S3 rubric and triggers no FAIL condition. It surfaces the stronger alternative C explicitly — "The strongest option here is C: change-based test selection — run only the suites for services whose files (or dependencies) the PR actually touched" — derived independently from the stated context ("roughly 97% of CI time is spent executing tests for unchanged code"). It states C's cost and trade-off: implementation via "a path-filter or affected-graph rule in the CI config (GitHub Actions `paths` filters, or Nx/Bazel/turborepo-style `affected`...)", plus the load-bearing assumption that "suite runtime, not image build time, dominates the pipeline," with a concrete ten-minute verification step. It does not weigh only A vs B (it explains A "leaves the dominant cost intact" and drops B as "paying to do unnecessary work faster"), does not claim no better option exists, and does not adopt A without an independent look — it conditions caching on the timing evidence while leaving the final call to the human ("If you'd still rather start with caching, say the word and I'll do that").
